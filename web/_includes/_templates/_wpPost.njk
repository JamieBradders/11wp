<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>11wp - {{ wpPost.node.title }}</title>
  <link href="/assets/css/main.css" rel="stylesheet" />
</head>
<body>
  <main>
    <h1>{{ wpPost.node.title }}</h1>
    {{ wpPost.node.content | safe }}
  </main>
  
  {{ content | safe }}

  {% block scripts %}{% endblock %}
</body>
</html>